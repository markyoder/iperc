#include "latticetype.h"



latticetype::latticetype(int nNeighbors) {
	// asume square lattice.
	init(0);
	}

//latticetype::latticeType(int ltype) {
//		 init(ltype);
//}

/*
latticetype::latticetype(std::string ltype) {
	 init(ltype);
}
*/

void latticetype::init() {
	 // various lattice types (use if or case)
	nDeltas = 
}
